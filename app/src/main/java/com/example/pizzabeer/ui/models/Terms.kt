package com.example.pizzabeer.ui.models

// if each element contains more data then this can be converted into a sealed class.
enum class Terms(val displayValue: String) {
    PIZZA("Pizza"), BEER("Beer")
}

